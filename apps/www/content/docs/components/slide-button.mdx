---
title: Slide Button
description: A stylish button with sliding text and arrow animation
component: true
links:
---

<ComponentPreview
  name="slide-button-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="Slide Button Demo"
/>

## Installation

<Tabs defaultValue="manual">

<TabsList>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="slide-button" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import SlideButton from "@/components/ui/slide-button"
```

```tsx
export default function SlideButtonDemo() {
  return (
    <div className="flex flex-col items-center justify-center">
      <SlideButton name1="Discover More" name2="Let's Go" />
    </div>
  )
}
```

## Props

The `SlideButton` component accepts the following props:

- `name1` (string): The text to display initially on the button.
- `name2` (string): The text to display when the button is hovered.
